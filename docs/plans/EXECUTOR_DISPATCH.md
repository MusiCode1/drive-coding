# Executor Dispatch — מצביע (מקור-האמת עבר ל-AGENTS.md)

> **הקובץ הזה אינו עוד מקור-אמת.** הוא שכפל את `AGENTS.md` ואת הגדרות הסוכנים, ונטה לדלף.
> מאז 2026-06-27 כל התוכן חי במקום אחד:

| מה אתה מחפש | איפה זה עכשיו |
|---|---|
| **env של הפרויקט** — worktree (`slice/<name>`), ports, OneCLI, hooks, commands | [`AGENTS.md`](../../AGENTS.md) (ה-`CLAUDE.md` עושה לו `@import`) |
| **הרצה / tunnel** (cloudflared, tuns.sh, mobile/Mic) | [`docs/running-locally.md`](../running-locally.md) |
| **פרוטוקול executor גנרי** — role, testing-strategy פר-commit, verifier, escalation, workflow | הגדרת הסוכן **eliezer** (מותקנת פר-מכונה) + סקיל brief-driven-slices |
| **gotchas פרויקטיים** (Svelte 5 reactivity, i18n, monorepo build) | `AGENTS.md` + `docs/` |

‏ה-brief §0 אמור להפנות ל-`AGENTS.md` (env) ולהגדרת הסוכן (פרוטוקול) — לא לקובץ הזה.
