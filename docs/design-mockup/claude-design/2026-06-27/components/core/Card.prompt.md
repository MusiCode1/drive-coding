`Card` is DriveCoding's elevated surface — bg-elev, hairline border, 14px radius.

```jsx
<Card>סוכן רגיל</Card>
<Card interactive onClick={open}>שורת סוכן עם hover</Card>
```

Set `interactive` for dashboard rows (hover lift to bg-card). `padding` and `as` are overridable.
