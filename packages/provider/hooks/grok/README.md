# Grok

No lifecycle hooks API for system-prompt injection (as of survey 2026-08-29).

Prefer ACP `session/new._meta.rules` (append). Do not install a no-op global
hook here — leave this folder for spawn/argv notes or a future API.
